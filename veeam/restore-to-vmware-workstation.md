# Veeam restore on VMWare workstation

To test restoring a veeam image to a vmware workstation image, add or modify the 
following to the machines config file while vmware is closed.

```bash
firmware = "efi"
```
